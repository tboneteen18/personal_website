<div class="container">
  <h1 class = "text-center">Rounds results for the tournament</h1>
  <h2> This round's winners</h2>
  <% @round.results.each do |result| %>
    <% next unless result.win == 1 %>
    <ul>
      <li> <%= link_to tournament_result_show_path do %>
           <%= result.user.first_name %> <%= result.user.last_name %>
           <%= result.user.win_count %> -
           <%= result.user.lose_count %>
        <% end %> </li>
    </ul>
  <% end %>
  <h2>This rounds losers</h2>
  <% @round.results.each do |result| %>
    <% next unless result.loss == 1 %>
    <ul>
      <li> <%= link_to tournament_result_show_path do %>
           <%= result.user.first_name %> <%= result.user.last_name %>
           <%= result.user.win_count %> -
           <%= result.user.lose_count %>
        <% end %> </li>
    </ul>
  <% end %>
  <p>
    need to add the page this should link to. <br>
    if you dont see a score then add a round number that isn't used. <br>
    I will be fixing this so it displays correctly. <br>
  </p>
</div>

<%= link_to tournament_round_index_path do%>
<i class="fa fa-times-circle fa-3x"></i>
<% end %>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

  ga('create', 'analytics_secret_code', 'auto');
  ga('send', 'pageview');

</script>
